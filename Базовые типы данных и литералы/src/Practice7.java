После выполнения кода переменная c получит значение –126,
 а переменные b и d вовсе не получат значений, потому что строки byte b = a + 10; и byte d = a + 1; вызовут ошибку компиляции:
выражения a + 10 и a + 1 имеют тип int, и Java не позволяет неявно приводить int к byte. 
Переменная c получает значение, потому что для неё указано явное приведение (byte)(a + 10),
 а результат выражения равен 130, что превышает диапазон byte (–128…127), поэтому происходит переполнение, и 130 превращается в –126.